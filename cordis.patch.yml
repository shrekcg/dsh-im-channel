# dsh-lark-bridge bundle patch: registers the plugin row so the package is
# installable via `dsh plugin add` (required by the awesome-dsh-plugin market).
- insert:
    - id: lark-bridge
      name: 'dsh-lark-bridge'
